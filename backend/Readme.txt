//use connect as we are using only one database or else use createconnection for multiple databases

//Await is a keyword paired with async functions that causes the JavaScript interpreter to pause 
//until the following asynchronous operation is run